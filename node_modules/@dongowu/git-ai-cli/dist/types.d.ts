export interface AIConfig {
    provider: string;
    apiKey: string;
    baseUrl: string;
    model: string;
    agentModel?: string;
    locale: 'zh' | 'en';
    customPrompt?: string;
    enableFooter?: boolean;
    outputFormat?: 'text' | 'json';
    rules?: CommitRules;
    rulesPreset?: string;
    fallbackModels?: string[];
    policy?: CommitPolicy;
    branch?: BranchConfig;
}
export interface CommitRules {
    types?: string[];
    scopes?: string[];
    scopeMap?: Record<string, string>;
    maxSubjectLength?: number;
    requireScope?: boolean;
    issuePattern?: string;
    issuePlacement?: 'scope' | 'subject' | 'footer';
    issueFooterPrefix?: string;
    requireIssue?: boolean;
}
export interface CommitPolicy {
    strict?: boolean;
}
export interface BranchConfig {
    types?: string[];
    pattern?: string;
    issueSeparator?: string;
    nameMaxLength?: number;
}
export interface ProviderPreset {
    name: string;
    baseUrl: string;
    defaultModel: string;
    requiresKey: boolean;
}
export declare const PROVIDER_PRESETS: Record<string, ProviderPreset>;
export declare const DIFF_BLACKLIST: string[];
export declare const MAX_DIFF_LENGTH = 5000;
//# sourceMappingURL=types.d.ts.map